[
  {"after_session": 61, "kind": "recall", "question": "How do I reach the mesa host from outside my home network?", "expected": "Through a Tailscale VPN into the host; the web site works over it but the iOS app does not connect (logged as a task)."},
  {"after_session": 64, "kind": "preference", "question": "Which design did I pick for the live conversation sidebar?", "expected": "The compact design, styled like the iOS app rather than animations in the header."},
  {"after_session": 65, "kind": "recall", "question": "What did I want so we could visualize things together during a conversation?", "expected": "A whiteboard that pops up beside the live chat, with anything worth keeping dropped into a project as an artifact or onto a task."},
  {"after_session": 68, "kind": "preference", "question": "What is my position on animations when reduced motion is on?", "expected": "All the animations should still work with reduced motion on; reduced motion stays on to save the phone's battery."},
  {"after_session": 70, "kind": "task-pointer", "question": "Which task covers showing a diff between two conflicting library items?", "expected": "Task 1097 on the mesa project."},
  {"after_session": 72, "kind": "task-pointer", "question": "Which task is the umbrella for getting the home-finance app into production?", "expected": "Task 1098."},
  {"after_session": 74, "kind": "preference", "question": "What must agents working on the home-finance app never touch, and why?", "expected": "The production database in the default location: the app now runs in production mode against real bank data via Plaid, so development must use the .env.sandbox setup and a separate db."},
  {"after_session": 74, "kind": "task-pointer", "question": "Which task makes the home-finance DB path follow the Plaid environment?", "expected": "Task 1103, high priority."},
  {"after_session": 76, "kind": "task-pointer", "question": "Which tasks cover the full-width home-finance UI and the cashflow redesign?", "expected": "Task 1104 (full-width UI) and task 1105 (cashflow redesign with a free-cash card, schedule card and projected balance chart)."},
  {"after_session": 77, "kind": "task-pointer", "question": "Which task covers managing categories in home finance?", "expected": "Task 1106."},
  {"after_session": 79, "kind": "recall", "question": "What did we do with the old spike folder when preparing auris for its public GitHub release?", "expected": "Stripped it out (the parakeet/whisper experiments), keeping the bench voice recordings."},
  {"after_session": 80, "kind": "recall", "question": "How is auris distributed?", "expected": "Through Homebrew, with a tag-triggered GitHub Actions release workflow mirroring khora, qorvex and mesa."},
  {"after_session": 81, "kind": "task-pointer", "question": "Which tasks cover SQL/KQL highlighting and CSV-as-table in the file viewer?", "expected": "Task 1109 (SQL and KQL highlighting) and task 1110 (CSV rendered as a table)."},
  {"after_session": 82, "kind": "recall", "question": "What did we agree for the library cards?", "expected": "Collapse each card to a single line, merge the duplicate supervisor entries into one row with a badge saying it overrides the built-in, and add a diff button against the built-in."},
  {"after_session": 83, "kind": "superseded", "question": "How does a release get pushed to GitHub?", "expected": "The person pushes the tag manually; mesa cannot push from its sandbox (the classifier blocks it) and the person wants a durable way to allow it.", "stale": "mesa pushes it itself, as authorized in the earlier release conversation."},
  {"after_session": 84, "kind": "task-pointer", "question": "Which tasks came out of the library only showing the stop-notify hook?", "expected": "Task 1114 (discover non-.sh hook files) and task 1115 (register/unregister hooks from the Library)."},
  {"after_session": 85, "kind": "task-pointer", "question": "Which task is the library sync conflict bug?", "expected": "Task 1116."},
  {"after_session": 86, "kind": "preference", "question": "What constraints did I set for the iosdl video downloader app?", "expected": "A personal, experimental app not going to the App Store; download any streaming video a page loads regardless of platform, but no DRM stripping."},
  {"after_session": 87, "kind": "superseded", "question": "Does the iosdl browser keep me logged in between launches?", "expected": "No: the person would rather log in every time, so persistence was removed and the browser uses a non-persistent data store.", "stale": "Yes, it uses a persistent website data store so cookies and logins survive launches."},
  {"after_session": 89, "kind": "preference", "question": "Should agent task summaries keep going to the inbox?", "expected": "No: the person no longer wants summaries in the inbox; only things that need someone's attention should land there, and agents should use it to message into the system."},
  {"after_session": 90, "kind": "preference", "question": "Should hook scripts use MESA_* environment variables or curly-brace placeholders?", "expected": "Curly-brace placeholders (replacements), consistently, never the environment variables."},
  {"after_session": 90, "kind": "preference", "question": "What should happen to the {bin} and {agent} placeholders in the hook templates?", "expected": "Remove them; the user sets the program and agent directly in each template."},
  {"after_session": 91, "kind": "superseded", "question": "How are hook scripts run: is a one-liner handled differently from a multi-line script?", "expected": "No: one mode only, everything runs as a bash script with the values shell-quoted in directly (task 1143).", "stale": "Two modes: a one-liner runs as argv and a multi-line value runs as a bash script, with values reaching it as MESA_* environment variables."},
  {"after_session": 92, "kind": "task-pointer", "question": "Which task makes the live window open automatically and be resizable?", "expected": "Task 1144."},
  {"after_session": 92, "kind": "recall", "question": "What did I report about mesa's voice playback?", "expected": "Speech cuts off after the first chunk; the person wore headphones so it is not mesa hearing itself; possibly CPU load from a Rust compile and kokoro running together."},
  {"after_session": 93, "kind": "preference", "question": "What do I think of the memory summaries mesa has been writing?", "expected": "They are too wordy and verbose; the person wants a better, bounded memory system."}
]
