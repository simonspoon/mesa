# pm-docs

PM documents for the mesa project (specs, design docs, consult notes), per
the Project Documents convention in the root CLAUDE.md. New specs land in
`specs/` here; documents written before this convention stay where they are
(`/specs` and `/.claude/consults` at the repo root).
